[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# Serve Next.js generated 404 page for missing routes
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
